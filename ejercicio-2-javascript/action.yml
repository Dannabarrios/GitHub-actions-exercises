name: 'Mi Accion JavaScript'
description: 'Accion que corre directamente en el runner'

inputs:
  nombre:
    description: 'Tu nombre'
    required: false
    default: 'Mundo'

outputs:
  mensaje:
    description: 'Mensaje generado'

runs:
  using: 'node20'
  main: 'index.js' 
